import React from 'react';
import ReactDOM from 'react-dom';

const root = document.getElementById("root")

if(root !== NULL) {
    ReactDOM.render(
        <h1>Hello, world!</h1>,
        root
      );
}
